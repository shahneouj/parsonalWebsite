---
const { src, alt, size, width, height } = Astro.props;
---

<img
  src={src}
  alt={alt}
  sizes={size}
  srcset={src}
  width={width}
  height={height}
  class="border-2 rounded-full inline-flex
  items-center justify-center"
/>
